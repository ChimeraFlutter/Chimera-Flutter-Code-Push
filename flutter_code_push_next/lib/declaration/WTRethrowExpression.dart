import 'package:flutter_code_push_next/InternalIndex.dart';

/// 访问重新表达
class WTRethrowExpression extends WTBaseDeclaration {
  @override
  dynamic execute(Environment env) {}

  @override
  void read(ByteArray byteArray) {
    super.read(byteArray);}
}
